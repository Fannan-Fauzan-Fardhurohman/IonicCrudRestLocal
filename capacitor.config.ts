import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fanxan.mahasiswa',
  appName: 'mahasiswa app',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
